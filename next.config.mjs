/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['homeopathyandwellness.com'],
    },
  };
  
  export default nextConfig;
  
//   /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;